# Tracked changes sample

The dose was {~~10 mg~>20 mg~~} administered {++twice ++}daily, and the
{--preliminary --}results were consistent across both cohorts.

This sentence has an insertion at the end{++ that a reviewer suggested++}.

CriticMarkup also supports {==highlighted text==} and inline critic notes
{>>should we cite the 2025 paper here?<<} that render as literal syntax in
dumb viewers — an accepted trade-off, since the notation stays plain text,
git-diffable, and unambiguous to an AI reading the raw file.

Turn on **Suggest** (Ctrl+Shift+E) and edit this paragraph: typing inserts
green suggested text, backspace strikes text red instead of removing it, and
typing over a selection makes a substitution. Place the cursor in any change
and use ✓ / ✗ to accept or reject it, or ✓ All / ✗ All for the whole file.
